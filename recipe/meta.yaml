{% set version = "0.13" %}

package:
  name: r-repr
  version: {{ version }}

source:
  fn: r-repr-{{ version }}.tar.gz
  url: https://github.com/IRkernel/repr/archive/{{ version }}.tar.gz
  sha256: 0cba9aa18906df46d567331f1cafd142fbda75cfb98e515a220b98d2e9a0f6f3

build:
  number: 0
  skip: True  # [win32]
  script: R CMD INSTALL --build .
  rpaths:
    - lib/R/lib/
    - lib/

requirements:  # [not win32]
  build:  # [not win32]
    - r-base  # [not win32]
    - posix  # [win64]
    - m2-git  # [win64]
  run:  # [not win32]
    - r-base  # [not win32]

test:
  commands:
    - R -e "library('repr')"  # [not win]
    - R -e \"library('repr')\"  # [win]

about:
  home: https://github.com/IRkernel/repr
  license: GPL-3
  summary: 'String and binary representations of objects for several formats / mime types.'

extra:
  recipe-maintainers:
    - ocefpaf
